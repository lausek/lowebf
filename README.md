# lowebf

## Directory structure

- `cache/`
    - `thumbs/`:
    - `twig/`:
    - `.htaccess`:
- `data/`
    - `config/`:
    - `content/`:
    - `media/`:
- `site/`
    - `public/`: scripts callable by anyone
    - `protected/`: scripts only callable if supplied with a shared secret
